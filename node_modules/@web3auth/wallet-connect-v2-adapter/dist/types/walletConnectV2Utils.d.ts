import type { ISignClient } from "@walletconnect/types";
import type { AddEthereumChainParameter, IProviderHandlers } from "@web3auth/ethereum-provider";
export declare function sendJrpcRequest<T, U>(signClient: ISignClient, chainId: number, method: string, params: U): Promise<T>;
export declare function getAccounts(signClient: ISignClient): Promise<string[]>;
export declare function getProviderHandlers({ connector, chainId }: {
    connector: ISignClient;
    chainId: number;
}): IProviderHandlers;
export declare function switchChain({ connector, chainId, newChainId, }: {
    connector: ISignClient;
    chainId: number;
    newChainId: string;
}): Promise<void>;
export declare function addChain({ connector, chainId, chainConfig, }: {
    connector: ISignClient;
    chainId: number;
    chainConfig: AddEthereumChainParameter;
}): Promise<void>;
